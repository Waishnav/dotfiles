# Always-on blue light filter
# Lower = warmer/more orange, Higher = cooler/less intense
# 6500K = neutral daylight, 4000K = warm, 3000K = very warm
# Note: hyprsunset >=0.4.0 uses profile-based config (old flat `temperature =` no longer parsed)
profile {
    time = 00:00
    temperature = 5000
}
